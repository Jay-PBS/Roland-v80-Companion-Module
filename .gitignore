node_modules/
package-lock.json
/pkg
# Built module packages. None is committed - they are build artifacts, and from 1.0 each
# release carries its .tgz as a GitHub Release asset instead.
/*.tgz
/dist
DEBUG-*
/.yarn
/.vscode
TESING NOTES.xlsx

# Test sheets and the code review stay on disk but are not tracked (CODE_REVIEW §4.3, 0.9.1).
# working_doc.md and PROTOCOL.md stay tracked.
/TESTING.md
/TESTING-NEXT.md
/CODE_REVIEW.md

# Reference material is Roland's, and stays local rather than being redistributed from this
# repo. That includes the LAN/RS-232 command doc, tracked until 1.0.2 - it is Roland's own
# download, and PROTOCOL.md records everything the module relies on. Also here: Roland's GPL
# source release (~445MB) and the 20MB reference manual.
/Reference Docs/
