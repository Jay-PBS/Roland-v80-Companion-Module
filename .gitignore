node_modules/
package-lock.json
/pkg
# Built module packages. None is committed - they are build artifacts, and from 1.0 each
# release carries its .tgz as a GitHub Release asset instead.
/*.tgz
/dist
DEBUG-*
/.yarn
/.vscode
TESING NOTES.xlsx

# Test sheets and the code review stay on disk but are not tracked (CODE_REVIEW §4.3, 0.9.1).
# working_doc.md and PROTOCOL.md stay tracked.
/TESTING.md
/TESTING-NEXT.md
/CODE_REVIEW.md

# Reference material: keep the small LAN/RS-232 command doc, exclude the bulk.
# Roland's GPL source release (~445MB, two files exceed GitHub's 100MB limit)
# and the 20MB reference manual are kept locally only.
/Reference Docs/v80 source code/
/Reference Docs/V-80HD_Reference_eng03_W.pdf
